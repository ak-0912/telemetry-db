apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetry-db.postgresServiceName" . }}
  labels:
    {{- include "telemetry-db.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
spec:
  type: ClusterIP
  ports:
    - name: postgresql
      port: 5432
      targetPort: postgresql
  selector:
    {{- include "telemetry-db.postgresLabels" . | nindent 4 }}
